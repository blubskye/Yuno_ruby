# frozen_string_literal: true

source 'https://rubygems.org'

# Discord API library
gem 'discordrb', '~> 3.5'

# Database
gem 'sqlite3', '~> 1.6'

# Environment variables
gem 'dotenv', '~> 2.8'

# JSON parsing (included in stdlib but explicit for clarity)
gem 'json', '~> 2.6'

group :development do
  gem 'rubocop', '~> 1.50'
end
